import { ElementRef, Renderer, OnChanges, SimpleChange } from '@angular/core';
/**
 * Directive to display custom icons from generated icon font.
 *
 * Example usage:
 * @usage
 * ```html
 * <custom-icon set="mySet" name="myIcon"></custom-icon>
 * ```
 *
 * @author Gerrit Erpenstein
 */
export declare class CustomIcon implements OnChanges {
    private _elementRef;
    private _renderer;
    /**
     * Icon name
     */
    iconName: string;
    /**
     * Icon set name
     */
    iconSet: string;
    /**
     * Current active css class
     */
    private _class;
    constructor(_elementRef: ElementRef, _renderer: Renderer);
    /**
     * Called on input parameter value changes.
     */
    ngOnChanges(changes: {
        [key: string]: SimpleChange;
    }): void;
    /**
     * Update and set element's css class value.
     * @private
     */
    private _update();
}
